// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes añadir configuraciones de Next.js si las necesitas en el futuro
  // Por ejemplo:
  // reactStrictMode: true,
  // images: {
  //   domains: ['example.com'], // Si usas imágenes externas
  // },
};

module.exports = nextConfig;